# String Calculator

Java implementation of the String Calculator kata with an interactive CLI. Supports comma/newline separators, custom delimiters, validation, negative-number reporting, ignored values over 1000, and JUnit 5 tests.

```bash
mvn test
java -cp target/classes Main
```
